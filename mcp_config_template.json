{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "c:/Users/fmonf/Desktop/Software Enginnering LAPTOP/Agencia B2B/agencia-web-b2b"
      ],
      "description": "Exploración profunda de archivos y estructura del repositorio."
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "TÚ_TOKEN_AQUÍ"
      },
      "description": "Análisis de historial de commits, PRs y gestión de repositorios."
    },
    "semgrep": {
      "command": "python",
      "args": ["-m", "semgrep", "mcp", "scan"],
      "description": "Escaneo de seguridad en tiempo real para vulnerabilidades OWASP."
    },
    "sonarqube": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sonarqube"],
      "env": {
        "SONAR_TOKEN": "TÚ_TOKEN_DE_SONARQUBE",
        "SONAR_HOST_URL": "http://localhost:9000"
      },
      "description": "Métricas de deuda técnica y calidad de código estático."
    },
    "sourcegraph": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sourcegraph"],
      "description": "Búsqueda semántica global en el código."
    }
  }
}
