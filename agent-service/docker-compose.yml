services:
  agent-service:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    restart: always
    volumes:
      - ./widget:/app/widget
    extra_hosts:
      - "host.docker.internal:host-gateway"
