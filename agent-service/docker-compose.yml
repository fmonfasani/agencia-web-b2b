services:
  agent-service:
    build: .
    ports:
      - "3001:3001"
    env_file: .env
    restart: always
    volumes:
      - ./widget:/app/widget
    extra_hosts:
      - "host.docker.internal:host-gateway"
